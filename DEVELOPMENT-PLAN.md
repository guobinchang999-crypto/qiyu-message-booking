# 栖愈预约系统开发计划

## 1. 当前前提

当前测试环境、后端数据库和真实接口尚未搭建完成，因此本项目第一阶段所有接口交互统一采用 Mock 数据方式实现。

开发目标不是先追求真实后端联调，而是先完成：

- 客户端小程序可演示预约闭环。
- 管理后台可演示运营、预约、排班和资源管理核心页面。
- 后端接口契约、状态流转和领域结构提前稳定。
- 后续真实接口接入时，可以平滑替换 Mock Service。

## 2. 总体开发顺序

项目按以下顺序推进：

```text
项目基础与 Mock 契约
→ 微信小程序客户端 UI 与预约闭环
→ 管理后台 UI 与运营流程
→ 后端接口骨架与领域模型
→ 前后端接口联调
→ 测试环境部署与验收
```

优先做能看、能点、能演示的完整业务闭环，再补真实存储、权限、支付、报表和部署能力。

## 3. 阶段一：项目基础与 Mock 契约

### 目标

建立三个模块的基础工程结构，并统一前后端 Mock 数据口径。

### 开发内容

- 创建 `qiyu-client` 微信小程序工程。
- 创建 `qiyu-admin` 管理后台工程。
- 创建 `qiyu-server` Spring Boot 后端工程骨架。
- 建立统一业务类型定义，包括门店、服务项目、技师、房间、排班、时间槽、预约、订单、优惠券、会员权益。
- 建立统一 Mock 数据源，保证客户端、管理后台和后端接口文档中的示例数据一致。
- 明确 Mock 开关：
  - 小程序默认启用 Mock Service。
  - 管理后台默认启用 Mock API。
  - 后端可先提供内存 Mock Controller 或仅维护接口契约。

### 验收标准

- 三个模块目录存在并能启动基础页面或基础服务。
- Mock 数据覆盖静安寺店、徐家汇店、陆家嘴店。
- Mock 数据覆盖肩颈舒缓、中式推拿、精油 SPA。
- Mock 数据覆盖待支付、已预约、已签到、待服务、服务中、待结算、已完成、已取消状态。
- 客户端和管理后台展示的数据语义一致。

## 4. 阶段二：微信小程序客户端

### 目标

根据 `design/` 下 15 张客户端 UI 图和设计规范，实现可演示的小程序预约闭环。

### 开发内容

- 使用微信小程序原生框架、TypeScript、TDesign MiniProgram。
- 按 `design/UI-DESIGN-SYSTEM.md` 建立主题令牌、公共样式和业务组件。
- 实现 15 个页面：
  - 登录
  - 首页
  - 门店列表
  - 门店详情
  - 服务列表
  - 服务详情
  - 选择技师
  - 选择时间
  - 确认预约
  - 预约成功
  - 订单列表
  - 预约详情
  - 到店签到
  - 服务评价
  - 我的
- 首页推荐顺序固定为：常去门店 → 附近门店。
- 预约流程使用本地状态保存用户选择的门店、项目、技师、时间、优惠券和费用明细。
- 所有列表、详情、提交、签到、评价都通过 Mock Service 返回。
- 处理加载、空态、错误、禁用、Toast、Modal、骨架屏和安全区。

### 验收标准

- 微信开发者工具可以正常预览。
- 15 个页面完整存在，视觉接近设计图。
- 从登录到预约成功可以完整演示。
- 预约成功后可进入预约详情和到店签到。
- 已完成订单可以进入服务评价。
- 无真实接口依赖，断网或无测试环境时仍可演示。

## 5. 阶段三：管理后台

### 目标

使用 React + Ant Design Pro 实现总部运营和门店履约管理的主要后台页面。

### 开发内容

- 创建 `qiyu-admin` 管理后台工程。
- 使用 Ant Design Pro / Ant Design 5 组件体系。
- 实现基础登录和布局。
- 实现核心页面：
  - 总部运营看板
  - 门店管理
  - 预约管理
  - 到店核销
  - 服务订单
  - 排班管理
  - 技师管理
  - 服务项目
  - 房间管理
  - 客户管理
  - 会员管理
  - 优惠券
  - 经营报表
- 表格、筛选、抽屉、弹窗、状态标签和图表全部基于 Mock API。
- 预约管理支持模拟创建预约、修改时间、更换技师、分配房间、签到、取消订单。
- 排班与房间页面需要模拟资源冲突提醒。

### 验收标准

- 管理后台可以本地启动。
- 核心页面可以通过 Mock 数据完整展示。
- 预约、核销、排班、订单状态在后台和客户端保持一致。
- 不依赖真实后端服务。

## 6. 阶段四：后端接口骨架

### 目标

在真实测试环境未完成前，先搭建后端工程骨架、接口契约和核心领域模型。

### 开发内容

- 创建 `qiyu-server` Spring Boot 3 工程。
- 按 `skills/cola-architecture/SKILL.md` 使用 COLA Light 分层。
- 建立统一响应结构、分页结构、错误码和异常处理。
- 建立业务枚举：
  - BookingStatus
  - PaymentStatus
  - TherapistStatus
  - RoomStatus
  - TimeSlotStatus
- 建立接口契约：
  - 登录与用户信息
  - 门店列表与详情
  - 服务项目列表与详情
  - 技师列表与详情
  - 可预约时间查询
  - 创建预约
  - 预约详情
  - 订单列表
  - 到店签到与核销
  - 开始服务与完成服务
  - 评价提交
  - 管理后台看板、预约、排班、房间、技师和报表接口
- 在没有数据库前，使用内存 Mock Repository 或静态 Mock Provider 返回数据。
- 预约冲突、状态流转等核心规则先在 domain 中表达，便于后续接入真实数据库。

### 验收标准

- 后端服务可以本地启动。
- Knife4j 或接口文档可以查看主要 API。
- Mock 接口返回结构稳定。
- 前端后续可以通过切换配置从本地 Mock 改为后端 Mock API。

## 7. 阶段五：联调与真实接口替换

### 目标

测试环境具备后，将前端 Mock Service 逐步替换为真实后端接口。

### 开发内容

- 保留 Mock 模式，不删除 Mock 数据。
- 增加环境配置：
  - `mock`：本地演示模式
  - `dev`：测试环境接口
  - `prod`：生产环境接口预留
- 按模块逐步切换：
  - 基础数据：门店、服务项目、技师
  - 预约流程：时间槽、确认预约、预约详情
  - 到店履约：签到、核销、开始服务、完成服务
  - 订单与评价
  - 后台管理与报表
- 每切换一个接口，保留对应 Mock 回退能力。

### 验收标准

- 同一页面可在 Mock 和真实接口之间切换。
- 接口异常时有错误提示和重试入口。
- 前后端状态枚举一致。
- 核心预约闭环在测试环境跑通。

## 8. 阶段六：测试与验收

### 测试范围

- 小程序页面视觉验收。
- 小程序预约闭环验收。
- 管理后台运营流程验收。
- 后端接口契约验收。
- 状态流转和资源冲突验收。
- 弱网、空态、错误、重复提交和禁用状态验收。

### 核心场景

- 客户登录后进入首页，看到常去门店优先于附近门店。
- 客户选择门店、项目、技师、时间并提交预约。
- 预约成功后进入预约详情。
- 客户到店展示核销码并点击签到。
- 后台查看预约、完成核销、分配房间、开始服务、完成服务。
- 客户查看已完成订单并提交评价。
- 已约满时间不可选择。
- 技师或房间冲突时后台出现提醒。

### 验收资料

- `design/UI-ACCEPTANCE-CHECKLIST.md` 作为客户端 UI 验收依据。
- 后续补充接口验收清单和后台验收清单。

## 9. Mock 数据规则

- Mock 数据必须集中管理，不在页面组件中随意硬编码。
- 客户端、管理后台、后端 Mock API 使用同一套业务命名和状态枚举。
- Mock 数据应覆盖正常、空态、错误、禁用和加载状态。
- Mock 数据应覆盖常去门店、附近门店、热门项目、推荐技师、可约时间、约满时间、订单状态、会员权益和优惠券。
- Mock Service 方法命名应尽量贴近未来真实接口，例如 `getStores`、`getBookingDetail`、`createBooking`、`checkinBooking`。
- 后续接入真实接口时，不改变页面调用方式，只替换数据来源。

## 10. 里程碑建议

| 里程碑 | 内容 | 建议结果 |
| --- | --- | --- |
| M1 | 项目脚手架与 Mock 契约 | 三个模块可启动，Mock 数据统一 |
| M2 | 小程序客户端 | 15 页完成，预约闭环可演示 |
| M3 | 管理后台 | 核心后台页面完成，运营流程可演示 |
| M4 | 后端接口骨架 | Mock API 和接口文档可用 |
| M5 | 测试环境联调 | 核心流程使用真实接口跑通 |
| M6 | 验收修复 | UI、流程、状态、异常全部通过 |

## 11. 开发优先级

### P0 必须先完成

- 小程序首页、门店详情、服务详情、选择技师、选择时间、确认预约、预约成功。
- Mock Service 与统一类型定义。
- 预约状态枚举和状态标签。
- 管理后台预约管理、到店核销、排班与房间状态。

### P1 第二批完成

- 订单列表、预约详情、到店签到、评价、个人中心。
- 管理后台技师管理、服务项目、客户管理。
- 后端接口契约和 Mock Controller。

### P2 后续增强

- 会员、优惠券、套餐卡完整规则。
- 支付、退款和结算。
- 经营报表深度分析。
- 真实测试环境和部署流水线。

## 12. 开发约束

- 当前阶段所有接口交互默认使用 Mock。
- 不因为没有测试环境而阻塞 UI 和流程开发。
- 不接入真实支付。
- 不擅自增加复杂财务、结算或多经营主体规则。
- 不把 UI 效果图作为页面底图。
- 不跳过加载、空态、错误、禁用和重复提交状态。
- 不让客户端、后台和后端各自定义一套不同状态枚举。

## 13. 当前实施状态与后续完善项

### 已完成基线

- MySQL 8.0 使用 Flyway 管理结构基线和基础数据，已初始化系统用户、角色、权限、菜单、组织部门、数据字典和业务演示数据。
- 管理后台已覆盖运营页面和系统管理页面，系统管理接口使用真实数据库，并由服务端执行权限校验和审计。
- 微信小程序 15 个规划页面、预约选择、提交、签到、取消、支付占位、评价和图片上传入口已形成完整闭环。
- 门店、服务项目和技师图片已纳入 `qiyu-server/src/main/resources/media-seed`，MinIO 启用后可自动上传并回写数据库 URL。

### P0 上线前必须完善

- 为 MinIO 配置正式凭据、私有桶访问策略和受控公开读取方案，执行资源种子上传并验证客户端图片加载。
- 将仍使用内存 Mock 的预约、评价、会员及部分运营查询全部迁移到 MySQL Repository，Mock 仅保留在显式 `mock` 模式。
- 增加 OpenAPI 契约回归测试，覆盖后台与小程序调用的所有业务接口。
- 补齐登录失败、令牌失效、跨门店查询、越权详情、导出和敏感字段查看的安全测试。
- 完成管理后台桌面端和小程序常用机型的截图验收，记录并修复布局、空态和错误态问题。

### P1 生产化完善

- Redis 接入登录会话、验证码、幂等键和热点时间槽缓存，明确 TTL 与失效策略。
- 增加请求追踪 ID、结构化日志、指标、健康检查和关键业务告警。
- 增加数据库备份恢复演练、Flyway 发布检查和 MinIO 生命周期/备份策略。
- 建立 CI 流水线，固定执行后端测试、后台类型检查与构建、小程序全量校验、DDL 校验和依赖漏洞扫描。
- 支付、退款、套餐核销和财务结算继续保持接口边界，取得真实业务规则及支付资质后再接入。
