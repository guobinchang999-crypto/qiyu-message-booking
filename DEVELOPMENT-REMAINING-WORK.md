# 栖愈预约系统剩余开发工作清单

> 更新时间：2026-08-14  
> 依据：`DEVELOPMENT-PLAN.md`、当前源码结构与本地验证结果。

## 1. 当前状态

### qiyu-client

- 已注册并具备源码的客户端页面：15/15。
- 本轮补齐：门店列表、服务列表、服务评价。
- 本轮接通：首页到门店列表/服务列表，门店列表到门店详情，服务列表到服务详情，已完成订单到评价页。
- 本轮推进：新增 `qy-status-view`、`qy-image`、`qy-bottom-action`、`qy-navbar`、`qy-store-card`、`qy-service-card`、`qy-order-card` 公共组件，并接入首页、门店列表、服务列表、服务详情、门店详情、订单列表、预约详情、选择技师、选择时间、确认预约、到店签到和服务评价等关键页面。
- 本轮推进：订单状态、订单动作、服务分类、评价标签、可约日期和时段改为通过客户端 Mock Service 返回，页面不再直接维护这些字典文案。
- 本轮推进：确认预约、到店签到、服务评价、选择技师、选择时间、首页、订单列表、服务详情、门店详情、预约详情和预约成功已补齐加载、错误、空态、重试或提交中禁用逻辑，避免重复提交和失败后无法恢复。
- 本轮推进：门店、服务和技师已增加真实图片资源字段入口，卡片、订单缩略图、服务详情横幅、门店详情横幅和技师头像均通过 `qy-image` 渲染，Mock 阶段缺图时自动回落占位。
- 本轮推进：登录页补齐提交中禁用和安全区 padding，预约成功页、到店签到页补齐底部安全区内容留白。
- 本轮推进：确认预约页改为通过 Mock Service 获取门店、服务、技师、预约时间和费用聚合摘要，页面不再硬编码确认信息和金额。
- 本轮推进：选择时间页摘要复用预约草稿聚合数据，选择技师按草稿中的服务项目查询；服务列表门店切换标签集中到 Mock Service 字典。
- 本轮推进：新增客户端 `mock/dev/prod` 环境配置、HTTP 请求封装、远程服务骨架和统一 `bookingService` 出口，页面不再直接依赖 `mockService`；默认仍为 Mock 模式。
- 本轮推进：客户端远程服务已补齐后端 Mock API 响应映射层，门店、服务、技师、时间槽和预约对象统一转换为小程序领域模型，并处理本地 Mock id 与后端 Mock id 的临时差异。
- 本轮推进：预约详情页和到店签到页的预约码改为使用预约对象 `code`，避免成功页、详情页和签到页展示不一致；签到按钮根据预约状态禁用。
- 本轮推进：成功页、门店详情、服务详情、评价页和通用卡片组件已清理 `as any`，页面状态改为明确领域类型。
- 本轮推进：服务详情页的服务流程、适合人群，以及门店详情页的空间亮点和会员权益文案已下沉到领域模型与 Mock/Remote Service 映射层；页面只负责渲染服务层返回的数据。
- 本轮推进：选择时间页的号源状态文案和图例改为通过时间字典返回；确认预约/选择时间中的技师展示名统一由预约确认聚合摘要提供，避免页面各自硬编码自动分配文案。
- 本轮推进：首页运营文案、快捷入口、区块标题和会员卡摘要已纳入 `HomePayload.copy`；服务列表搜索占位和横幅文案已纳入服务字典；确认预约页的权益、费用行、合计标题和协议文案已纳入预约确认聚合摘要；技师选择页的模式文案、自动分配提示和指定费展示前缀已纳入技师字典。
- 本轮推进：预约成功页的成功提示、预约编号前缀、到店提醒和操作按钮文案已纳入 `SuccessCopy`；我的页改为通过 `ProfilePayload` 获取用户信息、会员权益、快捷入口和菜单，并补齐加载、错误和重试状态。
- 本轮推进：到店签到页的标题、二维码说明、进度步骤、按钮状态文案、Toast 和底部动作已纳入 `CheckinDictionaryPayload`；预约详情页的状态步骤、预约码标题/提示、信息字段标签和签到按钮文案已纳入订单字典。
- 本轮推进：确认预约页的表单字段、输入占位和联系人必填提示已纳入预约确认聚合摘要；服务列表页标题已纳入服务字典；门店详情页操作项、推荐服务区标题和主按钮文案已纳入门店详情字典。
- 本轮推进：后端新增 `/api/v1/catalog/client`，统一返回小程序首页运营文案、订单字典、服务字典、门店详情字典、时间字典、技师字典、成功页文案、我的页权益和签到字典；客户端 `remoteService` 已优先读取该接口，失败时回落 Mock Service。
- 本轮推进：后端新增 `/api/v1/bookings/confirmation`，返回确认预约聚合摘要、费用预估、表单文案和权益文案；客户端 `remoteService.getBookingConfirmation` 已优先调用该接口，失败时回落 Mock Service。
- 本轮推进：后端新增 `/api/v1/member/profile`，返回我的页用户信息、会员权益、快捷入口和菜单；客户端 `remoteService.getProfile` 已优先调用该接口，失败时回落 catalog/mock。
- 本轮推进：后端新增 `/api/v1/bookings/{id}/success`，返回预约成功页所需的预约结果和成功页文案；客户端成功页改为通过 `remoteService.getBookingSuccess` 获取聚合摘要，失败时回落 Mock Service。
- 本轮推进：新增 `scripts/smoke-api.mjs`，覆盖小程序远程联调关键接口：统一 catalog、门店、服务、技师、时间槽、确认预约摘要、创建预约、预约详情、预约成功摘要和我的页资料；README 已补充执行方式。
- 本轮推进：自定义 TabBar 的“预约”入口改为服务列表页，选择时间页回归预约流程页，避免 TabBar 与固定底部下一步操作栏叠加；TabBar 同步补齐当前页选中同步、重复点击拦截和稳定触控区域。
- 本轮推进：登录页补齐验证码获取、发送中禁用、60 秒倒计时、验证码格式校验和错误提示，满足登录表单的可演示状态反馈。
- 本轮推进：确认预约页补齐服务人数步进交互、1-4 人边界禁用、人数变更后的费用摘要刷新和提交锁定；客户端 Mock Service 与后端 `/api/v1/bookings/confirmation` 已按人数计算服务金额、指定技师费和订金。
- 本轮推进：服务评价页补齐评分提交中锁定、满意标签最多 3 个、评价内容最小长度校验、页面内校验错误提示、提交成功后的重复提交保护和订单页跳转。
- 本轮推进：服务评价页补齐图片选择、预览、删除、上传中、上传失败和失败重试 Mock UI；`ReviewSubmitRequest` 已增加 `imageUrls`，提交时只发送成功上传的图片地址。
- 本轮推进：我的页补齐最近预约卡片，复用现有预约列表数据并支持跳转预约详情；退出登录改为二次确认，不清理预约记录。
- 本轮推进：门店列表页补齐常去优先/距离优先/评分优先排序、营业中筛选联动、定位拒绝/失败 Mock 状态提示和地图入口，默认排序显式保持常去门店优先。
- 本轮推进：服务列表页补齐门店切换跳转、搜索清空入口、推荐/销量/会员价排序和分类/搜索/排序联动，空结果后可快速恢复。
- 本轮推进：服务详情页补齐收藏/分享反馈、标签展示、当前门店摘要、注意事项、推荐技师和评价列表，固定“选择技师”入口保持原流程。
- 本轮推进：门店详情页补齐图片页码、导航/电话/收藏反馈、设施/营业时间/预约须知展开收起，以及项目/技师/评价 Tabs 联动内容。
- 本轮推进：服务详情首屏补齐从服务领域数据渲染的标题、标签和描述叠加层，服务详情与门店详情评价分页按钮改用独立的“加载更多评价”字典；详情加载先获取展示字典，失败态不再因并行请求提前失败而丢失错误文案。
- 本轮推进：修正远程服务映射，服务分类、销量、标签、流程、适合人群和图片字段全部由 `/api/v1/services` 返回并映射到客户端领域模型；后端服务分类字典改为从同一服务目录动态生成，Mock/dev 模式不再出现分类和详情数据漂移。
- 本轮推进：首页、门店、服务、订单和我的页启用微信下拉刷新，统一重新加载页面数据并在完成/失败后停止刷新；新增 `smoke:pull-refresh` 验证页面配置和刷新处理器。
- 本轮推进：管理后台 `AdminLayout` 增加 Mock 登录态守卫，未持有有效 `qiyu-admin-auth` 时重定向到登录页，并新增 `qiyu-admin/npm run smoke:auth-guard`。
- 本轮推进：后端补齐 `POST /api/v1/bookings/{id}/start-service`、`finish-service` 和 `settle`，状态规则由预约领域对象显式约束；MockMvc 与 `scripts/smoke-api.mjs` 已覆盖签到 → 服务中 → 待结算 → 已完成闭环。
- 本轮推进：后端时间槽查询改为按服务时长、准备/清洁缓冲和技师时间重叠判断占用；创建/改期的 Mock 冲突检查与保存进入应用服务临界区，并补充冲突创建、冲突改期和原预约不变测试。
- 本轮推进：客户端 `apiConfig` 不再永远固定为 Mock；通过 `qiyu-api-mode` 本地存储支持 `mock/dev/prod` 运行时选择，默认和非法值继续回落 Mock，并新增 API 模式 smoke 与 DevTools 切换说明。
- 本轮推进：新增后端 Mock 验证码/登录接口 `/api/v1/auth/send-code`、`/api/v1/auth/login`，客户端登录页改为通过 `BookingService` 请求验证码和登录，并统一保存返回 token；真实微信登录、短信供应商和 token 持久化仍待后续替换。
- 本轮推进：我的页退出登录会清理统一认证 token 后再返回登录页，避免切换账号时复用旧身份。
- 本轮推进：选择技师页补齐可约/不可约状态展示、不可约禁选提示、显示全部/仅看可约切换和底部已选摘要，指定技师与自动分配保持互斥。
- 本轮推进：选择时间页补齐日期选择、时段切换后槽位联动、已约满点击提示、当前选择底部摘要，以及无可约时间时的换日期/更换技师入口。
- 本轮推进：订单列表页补齐完整状态 Tabs、待支付/已取消 Mock 样例、继续支付/取消预约/再次预约动作入口和取消后的页面内状态同步。
- 本轮推进：预约详情页补齐更完整状态 Steps 映射、预约码过期/刷新说明、支付信息区块，以及基于 `availableActions` 的导航/联系/改期/取消/支付/评价/再次预约等操作入口。
- 本轮推进：新增后端取消预约接口 `POST /api/v1/bookings/{id}/cancel`，领域对象显式执行 `cancel()` 状态流转；订单列表页和预约详情页已通过 `bookingService.cancelBooking()` 接入取消动作，Mock Service 保留本地状态回落。
- 本轮推进：新增后端 Mock 支付确认接口 `POST /api/v1/bookings/{id}/pay`，领域对象显式执行 `payDeposit()` 状态流转；订单列表页和预约详情页已通过 `bookingService.payBooking()` 接入待支付预约的订金支付确认动作。
- 本轮推进：新增后端再次预约草稿接口 `GET /api/v1/bookings/{id}/rebook-draft`，返回来源预约对应的可编辑预约草稿；订单列表页和预约详情页已通过 `bookingService.getBookingRebookDraft()` 写入 `bookingStore` 后进入服务详情页继续预约。
- 本轮推进：新增后端改期草稿接口 `GET /api/v1/bookings/{id}/reschedule-draft`，返回当前预约对应的可编辑时间调整草稿；订单列表页和预约详情页已通过 `bookingService.getBookingRescheduleDraft()` 写入 `bookingStore` 后进入选择时间页。
- 本轮推进：新增后端改期提交接口 `POST /api/v1/bookings/{id}/reschedule`，复用现有时间选择与确认页；`BookingDraft` 增加 `flow/sourceBookingId`，确认页会按流程调用创建预约或改期提交。
- 本轮推进：到店签到页补齐签到码刷新入口、刷新后提示、非可签到状态重复点击提示，以及底部导航/联系动作反馈。
- 本轮推进：预约成功页补齐预约码说明、导航/联系门店反馈和错误态返回首页兜底文案，查看详情/返回首页动作保持可用。
- 本轮推进：首页补齐快捷入口 Mock 反馈、会员权益卡跳转我的页、附近门店排序入口反馈，确保门店、项目、权益入口都有明确点击响应。
- 本轮推进：确认预约页补齐门店、项目、技师、时间修改入口，分别回到对应选择页并保持当前预约草稿。
- 本轮推进：新增 `ActionFeedbackDictionaryPayload` 和 `bookingService.getActionFeedbackDictionaries()`，首页、门店、服务、预约、签到等页面的导航/联系/客服/分享/Mock 操作/人数边界/号源不可选等通用 Toast 文案已从页面下沉到 service/mock 层。
- 本轮推进：新增 `PageStateDictionaryPayload` 和 `bookingService.getPageStateDictionaries()`，首页、门店列表、服务列表和订单列表的加载/错误/空态文案，以及门店列表搜索、筛选、定位提示等页面状态文案已从 WXML/页面代码下沉到 service/mock 层。
- 本轮推进：`PageStateDictionaryPayload` 继续覆盖确认预约、预约详情、选择技师、选择时间、到店签到、服务评价、预约成功和我的页，相关加载/错误/空态/流程提示文案已改为从 `bookingService.getPageStateDictionaries()` 获取。
- 本轮推进：门店详情页的加载/错误、收藏态、展开面板、Tabs、营业时间/预约须知和评价说明文案已纳入 `StoreDetailDictionaryPayload`；服务详情页的状态文案、收藏/分享、价格标签、介绍/流程/适合人群/注意事项、推荐技师、评价说明和主按钮文案已纳入 `ServiceDictionaryPayload`。
- 本轮推进：预约详情页支付区块标题、支付字段标签和可用操作标题已纳入 `OrderDictionaryPayload`；服务评价页评分项、输入占位、图片上传提示、匿名/确认选项、提交按钮、提交反馈和校验提示已纳入 `ReviewDictionaryPayload`。
- 本轮推进：新增 `LoginCopyPayload` 和 `bookingService.getLoginCopy()`，登录页品牌、表单、验证码、协议和按钮文案改为从 service/catalog 获取；我的页退出登录弹窗文案已纳入 `ProfilePayload`。
- 本轮推进：我的页最近预约标题/动作、预约详情页标题和预约码补充说明、预约成功页预约码说明/导航/联系动作、选择技师页标题和可约状态标签已继续纳入 `ProfilePayload`、`OrderDictionaryPayload`、`SuccessCopy` 和 `TherapistDictionaryPayload`，并同步到后端 catalog/profile 接口。
- 本轮推进：确认预约页标题和门店/项目/技师/时间修改动作已纳入预约确认摘要；订单列表页标题、结构化状态 Tabs、取消预约弹窗和取消成功 Toast 已纳入 `OrderDictionaryPayload`，页面不再用中文 Tab 文案做业务过滤。
- 本轮推进：门店列表排序项已纳入 `StoreListStateCopy.sortOptions`；服务列表排序项和搜索清除按钮已纳入 `ServiceDictionaryPayload`，页面不再维护这些展示文案。
- 本轮推进：`qy-store-card`、`qy-service-card` 和 `qy-order-card` 中的评分单位、最近可约前缀、服务时长单位、已服务文案和实付前缀已改为通过页面字典 `metaCopy` 透传，公共组件不再直接维护这些业务展示文案。
- 本轮推进：确认预约页、选择时间页、预约详情页和服务详情页中的服务时长单位、评分单位、已服务文案和选择时间标题继续下沉到预约确认摘要、订单字典、服务字典和页面状态字典，重点预约链路不再直接写这些动态单位文案。
- 本轮推进：选择技师页的从业年限、评分单位和服务次数文案已纳入 `TherapistDictionaryPayload.metaCopy`，页面只拼接后端/catalog 返回的单位和前后缀。
- 本轮推进：登录页 `emptyCopy` 已移除页面层中文兜底，登录品牌、表单、验证码、协议和校验文案只从 `LoginCopyPayload` / catalog 获取。
- 本轮推进：预约成功、确认预约、门店详情、选择时间、预约详情、我的、服务评价、服务详情、选择技师和到店签到等页面的 `emptyState` / 空字典中文错误兜底已清空，失败态和空态展示文案继续从 `PageStateDictionaryPayload` 或对应详情字典获取。
- 本轮推进：门店营业筛选从中文展示值判断改为 `businessStatusCode` 状态码判断，`businessStatus` 保留为展示标签；客户端 Mock、远程映射和后端 `/api/v1/stores` Mock 契约已同步补齐状态码与展示标签字段。
- 本轮推进：新增客户端 `constants/ui.ts` 集中维护图片/状态占位字，门店详情、服务详情、选择技师和公共卡片/状态组件的 WXML 模板不再直接写死 `栖/愈/疗` 等占位字符。
- 本轮推进：选择时间页的时间段筛选从中文值改为 `TimePeriodCode`，`timeDictionaries.periods` 改为 `{ key, label }` 结构；客户端 Mock、远程时间槽映射和后端 catalog 已同步使用 `MORNING/AFTERNOON/EVENING` 稳定码。
- 本轮推进：新增并扩展 `npm run validate:miniprogram`，静态检查 `app.json` 页面注册与 `pageRoutes` 双向一致性、页面/组件/自定义 TabBar 的 JS 输出存在且不落后于 TS 源码、本地公共组件文件完整性与 `component: true` 声明、自定义 TabBar 文件完整性、TDesign 1.15.x 锁定版本与 `miniprogram_npm` 构建产物、WXML 使用的自定义/TDesign 组件声明、组件路径可解析、`app.json` TabBar / `pageRoutes` / `tabBarItems` 路由一致性、页面/组件中文文案集中规则、页面路径字面量集中规则、跳转 API 与 TabBar/普通页面的兼容性规则和固定底部操作栏安全留白规则，提前拦截微信开发者工具中的组件缺失、导航漂移、文案散落、路由散落、跳转 API 误用、底部按钮遮挡和忘记生成 JS 产物类错误。
- 本轮推进：新增 `npm run smoke:product-boundary`，扫描客户端页面、组件、Mock、Service、类型、常量和工具代码，禁止出现门店类型、多经营主体、租户/商户/加盟、独立会员体系等字段或展示文案，保护栖愈统一品牌预约系统边界。
- 本轮修复：我的页已补齐 `qy-status-view` 组件声明，避免加载/错误状态在微信开发者工具中因组件未注册而渲染失败。
- 本轮推进：新增 `npm run smoke:login-form`，直接验证登录页品牌/验证码字典、手机号与 6 位验证码校验、协议勾选拦截、验证码 60 秒倒计时、发送/提交禁用和登录成功回首页跳转，保护登录到首页演示入口。
- 本轮推进：新增 `npm run smoke:home-order`，直接验证首页常去门店数据只包含 `isFrequent=true` 门店、附近门店不与常去门店重叠，并静态验证首页 WXML 渲染顺序保持“常去门店 → 附近门店 → 推荐项目”。
- 本轮推进：新增 `npm run smoke:store-location-actions`，直接验证门店导航/联系工具的 `wx.openLocation`、`wx.makePhoneCall` 调用参数与失败兜底文案，并静态验证门店列表定位、地图选点、排序筛选、空态和门店详情动作字典绑定。
- 本轮推进：新增 `npm run smoke:profile`，直接验证我的页会员权益保持“全门店通用”表达、余额/套餐/优惠券统计来自 `ProfilePayload`、最近预约可进入预约详情、订单入口使用 TabBar 跳转、退出登录需二次确认且不删除预约记录。
- 本轮推进：新增 `npm run smoke:order-status`，直接验证客户端 `BookingStatus` 类型、订单状态中文标签、订单 Tabs 过滤状态和 Mock 订单样例都对齐统一 8 个状态：待支付、已预约、已签到、待服务、服务中、待结算、已完成、已取消。
- 本轮推进：新增 `npm run smoke:booking-store`，直接验证预约草稿默认值、门店切换重置旧技师/时间段、项目切换重置旧技师/时间段、同值选择不重置以及订阅/取消订阅行为，保护首页、门店、服务、确认预约和改期链路的状态一致性。
- 本轮推进：新增 `npm run smoke:time-slot`，直接验证 Mock 时间槽覆盖已约满/可约/紧张状态、默认草稿不指向已约满槽位，并静态验证选择时间页会拒绝已约满槽位、恢复草稿时忽略已约满槽位、自动选择时只选择非约满槽位。
- 本轮推进：新增 `npm run smoke:booking-draft-flow`，直接验证再次预约草稿使用新建流程且不携带原预约提交 id、改期草稿保留 `sourceBookingId` 并要求重新选择时间，避免确认页把改期误提交成新预约或再次预约误覆盖原订单。
- 本轮推进：新增 `npm run smoke:confirm-submit`，直接验证确认页新建流程会新增预约并置顶订单列表、改期流程只更新原预约且不增加订单数量，保护确认预约页创建/改期提交分流。
- 本轮推进：新增 `npm run smoke:booking-actions`，直接验证预约码刷新会同步二维码 URL、到店签到会进入已签到并移除刷新/改期动作、取消预约会进入已取消并移除支付/取消动作，保护预约详情、订单列表和到店签到页的履约状态一致性。
- 本轮推进：新增 `npm run smoke:review-flow`，直接验证已完成订单提交评价后移除评价动作，并将新评价回流到对应门店与服务项目的评价分页列表，保护“订单完成 → 服务评价 → 详情评价展示”闭环。
- 本轮推进：新增 `npm run smoke:review-upload`，直接验证评价图片上传成功返回 URL、失败时抛错，并验证带成功上传图片地址的评价提交后可回流到服务评价列表，保护服务评价页图片上传与提交联动。
- 本轮推进：新增 `npm run smoke:payment`，直接验证 Mock 支付跳过 `wx.requestPayment`、小程序支付成功后确认支付、支付失败不确认订单支付，保护预约详情与预约成功后的支付确认路径。
- 本轮推进：新增 `npm run build:miniprogram-js` 和 `npm run verify`，统一串联类型检查、生成小程序 JS、静态预检、产品边界 smoke、登录表单 smoke、首页顺序 smoke、门店定位/导航/联系 smoke、我的页 smoke、订单状态 smoke、预约草稿 smoke、时间槽 smoke、草稿流程 smoke、确认提交 smoke、履约动作 smoke、评价 flow smoke、评价图片上传 smoke 和支付 smoke，后续客户端改动优先使用一个命令完成本地验证。
- 当前验证：`npm run verify` 通过，覆盖 `npm run typecheck`、`npm run build:miniprogram-js`、`npm run validate:miniprogram`、`npm run smoke:product-boundary`、`npm run smoke:login-form`、`npm run smoke:home-order`、`npm run smoke:store-location-actions`、`npm run smoke:profile`、`npm run smoke:order-status`、`npm run smoke:booking-store`、`npm run smoke:time-slot`、`npm run smoke:booking-draft-flow`、`npm run smoke:confirm-submit`、`npm run smoke:booking-actions`、`npm run smoke:review-flow`、`npm run smoke:review-upload` 和 `npm run smoke:payment`。

### qiyu-admin

- 已有可访问路由：登录、总部运营看板、门店管理、预约管理、到店核销、服务订单、排班管理、技师管理、服务项目、房间管理、客户管理、会员管理、优惠券、经营报表。
- 本轮修复：TypeScript `@/*` 路径别名、Umi 路由 hook 类型不匹配、预约保存对象覆盖错误。
- 本轮补齐：计划要求的后台核心页面与侧边栏入口，新增后台通用表格页组件和对应 Mock 数据。
- 本轮推进：后台 Mock API 改为模块级内存状态，预约、核销、服务订单和房间资源支持跨页面状态联动。
- 已支持的后台 Mock 流转：创建/修改预约、取消预约释放房间、确认签到、安排待服务、开始服务、完成服务进入待结算、完成结算释放房间。
- 当前验证：`npm run typecheck`、`npm run build`、`npm run smoke:auth-guard` 和 `npm run smoke:fulfillment` 通过。

### qiyu-server

- 已有 Mock API：健康检查、门店、服务项目、技师、时间槽、预约创建/列表/详情/签到、评价、后台看板/预约/排班资源。
- 本轮修复：Mockito 测试 mock maker 配置，避免当前 JDK 环境下 inline mock maker 自附加失败。
- 本轮推进：新增后端 catalog 字典/资源选项 Mock API，供前端后续获取状态文案、门店、服务项目、技师和房间选项。
- 本轮推进：新增小程序统一 catalog payload：`/api/v1/catalog/client`，覆盖客户端已集中到 service 层的主要字典和运营文案，并补充 MockMvc 结构测试。
- 本轮推进：新增预约确认摘要接口 `/api/v1/bookings/confirmation`，用于小程序确认页在创建预约前获取门店、服务、技师展示名、预约时间、费用明细和表单文案，并补充 MockMvc 聚合结构测试。
- 本轮推进：新增会员资料接口 `/api/v1/member/profile`，用于小程序我的页获取用户信息和全门店通用会员权益，并补充 MockMvc 结构测试。
- 本轮推进：新增预约成功摘要接口 `/api/v1/bookings/{id}/success`，用于小程序成功页一次性获取预约结果与成功页展示文案，并补充 MockMvc 结构测试。
- 本轮推进：`/api/v1/catalog/client` 补齐 `actionFeedbackDictionaries`，承接客户端通用动作反馈文案，避免最终联调时依赖页面硬编码或纯前端 Mock。
- 本轮推进：`/api/v1/catalog/client` 补齐 `pageStateDictionaries`，承接客户端主入口页面的加载、错误、空态和定位提示文案，并补充 MockMvc 结构断言。
- 本轮推进：后端 `pageStateDictionaries` 继续补齐预约确认、预约详情、选择技师、选择时间、到店签到、服务评价、预约成功和我的页状态文案，并新增时间页空态动作断言。
- 本轮推进：后端 `serviceDictionaries` 和 `storeDetailDictionaries` 补齐详情页区块、动作和说明文案，并补充 MockMvc 断言覆盖服务介绍标题与门店设施面板标题。
- 本轮推进：后端 `orderDictionaries` 补齐支付区块字段，新增 `reviewDictionaries` 承接服务评价页展示/校验文案和图片上传 UI 文案；客户端 `remoteService.getReviewDictionaries()` 已优先读取 catalog 并回落 Mock。
- 本轮推进：后端 `/api/v1/catalog/client` 新增 `loginCopy`；`/api/v1/member/profile` 和 catalog profile 均补齐退出登录弹窗字段，并补充 MockMvc 断言。
- 本轮推进：后端 `/api/v1/catalog/client` 和 `/api/v1/member/profile` 继续补齐客户端页面剩余展示字典，包括预约详情标题/预约码补充说明、技师可约状态、预约成功页门店动作和我的页最近预约文案，并补充 MockMvc 断言。
- 本轮推进：后端预约确认摘要新增 `pageTitle` 和 `editActions`；`orderDictionaries` 新增订单页标题、结构化 Tabs 和取消预约弹窗/Toast 字段，并补充 MockMvc 断言。
- 本轮推进：后端 `serviceDictionaries` 新增服务列表搜索清除文案和排序项；`pageStateDictionaries.stores` 新增门店列表排序项，并补充 MockMvc 断言。
- 本轮推进：后端统一 catalog 补齐首页、门店列表、门店详情、服务列表和订单列表所需的卡片 `metaCopy` 文案，并补充 MockMvc 断言。
- 本轮推进：后端预约确认摘要、订单字典、服务字典和时间页状态字典继续补齐动态单位/标题字段，并补充 MockMvc 断言。
- 本轮推进：后端 `therapistDictionaries` 补齐技师卡 meta 文案，并补充 MockMvc 断言。
- 本轮推进：预约确认摘要按服务人数计算服务金额、指定技师费和订金，并补充 MockMvc 断言覆盖 2 人预约费用。
- 本轮推进：后端新增取消预约接口并补充 MockMvc 状态流转断言；客户端远程服务已接入该接口，取消失败提示文案同步改为真实失败反馈。
- 本轮推进：后端新增 Mock 支付确认接口并补充 MockMvc 状态流转断言；订单字典新增支付成功/失败反馈文案，客户端远程服务已接入该接口。
- 本轮推进：后端新增再次预约草稿接口并补充 MockMvc 结构断言；客户端远程服务已接入该接口，避免订单页自行拼接复约草稿。
- 本轮推进：后端新增改期草稿接口并补充 MockMvc 结构断言；客户端远程服务已接入该接口，改期入口不再停留在通用 Mock 提示。
- 本轮推进：后端新增改期提交接口，领域对象支持 `reschedule()` 更新时间范围，并在提交前复用资源冲突校验；客户端 `remoteService.rescheduleBooking()` 和确认页提交分流已接入。
- 本轮推进：新增后端验证码刷新接口 `POST /api/v1/bookings/{id}/verification-code/refresh`，预约聚合维护 `verificationCode` 并限制仅已预约/已签到状态可刷新；到店签到页已通过 `bookingService.refreshBookingCode()` 接入真实刷新动作，Mock Service 保留离线演示回落。
- 本轮推进：预约详情页新增 `refresh_code` 动作入口，订单字典和 Mock/Remote 可用动作已同步；点击后直接调用 `bookingService.refreshBookingCode()` 并更新当前页预约码。
- 本轮推进：门店基础数据补齐电话和经纬度字段，客户端新增统一门店动作工具；门店详情、订单列表、预约详情、到店签到和预约成功页的联系门店/导航到店已接入 `wx.makePhoneCall` / `wx.openLocation`，失败时回落 catalog 反馈文案。
- 本轮推进：服务详情页分享入口改为小程序原生 `open-type="share"`，页面实现 `onShareAppMessage()` 返回当前服务详情路径，分享按钮样式与原有胶囊动作保持一致。
- 本轮推进：门店列表地图入口已接入 `wx.chooseLocation`，选点成功后按所选经纬度重算门店直线距离、展示所选位置并切换到距离优先排序；取消/失败时回落现有地图失败反馈。
- 本轮推进：门店列表重新定位入口已接入 `wx.getLocation`，定位成功后复用距离重算逻辑，定位拒绝/失败时展示对应页面状态；原模拟拒绝/失败按钮已移除。
- 本轮推进：门店详情页图片区域改为 `swiper` 轮播，支持手势滑动和页码联动；`Store` 模型新增 `galleryImageUrls` 承接后续多图资源，Mock 阶段继续通过占位图展示。
- 本轮推进：后端新增门店评价列表 Mock 查询 `GET /api/v1/reviews?storeId=...`，客户端新增 `StoreReview` 与 `bookingService.getStoreReviews()`；门店详情评价 Tab 已从静态概览改为真实列表渲染。
- 本轮推进：评价列表接口新增 `serviceId` 过滤，服务详情页已调用 `bookingService.getStoreReviews(storeId, serviceId)` 渲染当前服务的评价列表。
- 本轮推进：评价列表接口改为分页结构 `{ items, page, pageSize, total, hasMore }`，门店详情和服务详情评价列表已接入分页加载更多。
- 本轮推进：服务详情和门店详情的评价说明文案已更新为当前列表语义，并同步到客户端 Mock Service 与后端 catalog。
- 本轮推进：已由 subagent 完成 MySQL 8.0 DDL 与数据库设计说明：`qiyu-server/src/main/resources/schema-mysql8.sql`、`qiyu-server/src/main/resources/schema-notes.md`。
- 本轮推进：服务评价页图片上传从页面内 `mockUploadImage()` 下沉到 `bookingService.uploadReviewImage()`；后端新增 `POST /api/v1/reviews/images` 返回 mock CDN URL，客户端 Remote/Mock 均已接入上传状态和 URL 回填。
- 本轮推进：预约对象新增 `qrImageUrl`，后端预约视图返回 `verificationQrImageUrl`；预约详情页和到店签到页优先渲染二维码图片，加载失败时回退数字码占位，刷新预约码时同步刷新二维码图片地址。
- 本轮推进：首页快捷入口中的客服项改为小程序原生 `open-type="contact"`，非客服快捷入口继续走页面路由；客服能力不可用时保留 catalog 字典兜底提示。
- 本轮推进：支付入口从直接调用确认接口升级为“准备支付参数 → 小程序支付能力/Mock 跳过 → 后端确认”的两段式链路；新增 `bookingService.prepareBookingPayment()`、客户端 `payBookingDeposit()` 工具和后端 `POST /api/v1/bookings/{id}/payment` Mock 支付参数接口。
- 本轮推进：评价提交接口和客户端 Mock Service 已支持提交后追加评价记录；新评价会按预约所属门店/服务回流到门店详情和服务详情的评价分页列表，提交字段同步承接评分、标签、匿名和图片 URL。
- 本轮推进：`scripts/smoke-api.mjs` 扩展为覆盖近期客户端远程联调关键契约：支付参数准备/确认、预约码刷新、改期、取消、评价图片上传、评价提交回流和会员资料；README 已同步 smoke 覆盖范围。
- 本轮推进：修复服务评价提交成功后的回订单列表跳转，`/pages/orders/index` 属于 TabBar 页面，已由 `wx.redirectTo` 改为 `wx.switchTab`，避免小程序运行时跳转失败。
- 本轮推进：自定义 TabBar 的展示项和图标已集中到 `constants/navigation.ts`，`custom-tab-bar` 组件只消费配置，避免导航文案散落在组件实现内。
- 本轮推进：客户端页面路由已集中到 `constants/navigation.ts` 的 `pageRoutes/pageUrls`，首页、登录、确认预约、预约成功、订单列表、预约详情、服务详情、门店详情、门店/服务列表、选择技师、选择时间、服务评价和我的页的高频跳转不再直接散落 `/pages/...` 字符串。
- 本轮推进：门店列表、门店详情与预约草稿已接通；选择门店会通过 `bookingStore.selectStore()` 更新当前门店，并在门店变化时重置旧技师和旧时间段，避免确认预约时提交跨门店脏数据。
- 本轮推进：通用导航栏返回首页兜底路径已接入 `constants/navigation.ts`，客户端页面和组件中的页面路径继续保持集中维护。
- 本轮推进：选择技师页加载时会优先恢复预约草稿中的自动/指定模式和已选技师；选择时间页会优先恢复草稿中的可用时段，并同步切换到该时段所在的时间段 Tab，改期/修改流程返回时不再丢失当前选择。
- 本轮推进：选择技师页和选择时间页针对“底部摘要 + 固定主按钮”的双固定区域增加页面底部留白，降低最后一张卡片或号源被固定区域遮挡的风险。
- 本轮推进：首页门店卡和项目卡已接入预约草稿；从首页进入门店详情会写入当前门店，从首页进入服务详情会写入当前项目。
- 本轮推进：新增 `bookingStore.selectService()`，服务项目变化时统一重置旧技师和旧时间段；首页、门店详情、服务列表和服务详情的项目选择入口已统一接入，避免换项目后提交旧资源选择。
- 本轮推进：订单列表页支付、取消、再次预约和改期动作增加页面级重复点击锁，避免快速连续点击导致重复请求或重复跳转。
- 本轮推进：预约详情页支付、取消、刷新预约码、再次预约和改期动作增加页面级重复点击锁；支付失败和预约码刷新失败 Toast 已改为使用对应字典文案。
- 本轮推进：到店签到页刷新预约码增加独立锁，并与签到中状态互斥，避免签到和刷新码并发导致页面展示与后端状态不一致。
- 本轮推进：服务评价页提交前会检查是否仍有图片处于上传中；上传未完成时阻止提交并展示字典文案，避免刚选择的图片被静默漏提交。
- 本轮推进：公共 `qy-bottom-action` 的次按钮补齐 loading/disabled 防护和禁用态样式；后续页面接入次操作时不会绕过固定操作栏的提交锁。
- 本轮推进：预约详情页底部签到按钮已接入页面 `acting` 锁，支付、取消、刷新码、改期等动作进行中不能同时触发签到跳转。
- 当前验证：`mvn test`、`qiyu-client/npm run verify`、`qiyu-admin/npm run typecheck`、`qiyu-admin/npm run build` 和本地启动后的 `node scripts/smoke-api.mjs` 均通过；远程 API smoke 已覆盖服务目录、服务时长缓冲后的时间槽、预约创建和履约状态链路，客户端 verify 已增加下拉刷新和 15 页关键交互入口 smoke；后端同时覆盖履约非法状态拒绝和冲突预约拒绝。

## 2. P0 剩余工作

### 客户端预约闭环验收

- 使用微信开发者工具导入 `qiyu-client/`，执行构建 npm 并完成 15 页真机/模拟器预览。
- 导入微信开发者工具前先执行 `npm run verify`，确保类型检查、JS 输出新鲜度、页面注册与集中路由、本地公共组件、自定义 TabBar 文件、TDesign npm 构建产物、自定义/TDesign 组件声明、TabBar 路由一致性、跳转 API 与页面类型匹配、客户端无门店类型/多经营主体/独立会员体系暴露、登录表单校验与提交锁、首页常去门店优先、门店定位/地图选点/导航/联系动作绑定、我的页全门店通用权益和退出确认、订单状态枚举/标签/Tab 过滤一致、页面/组件文案集中、页面路径集中、固定底部操作栏安全留白、预约草稿重置规则、已约满时间不可选择、再次预约/改期草稿流程语义、确认预约创建/改期提交分流、预约码刷新/签到/取消履约状态、评价图片上传、评价提交回流和支付确认工具没有静态缺口。
- `smoke:page-acceptance` 已增加 15 个设计页面、四个 TabBar 入口，以及预约、定位、履约、评价和个人中心关键交互入口的源码级检查；这不能替代微信开发者工具的视觉/真机验收。
- 使用微信开发者工具重点检查首页、门店、服务、预约、订单、签到和评价链路的视觉还原、滚动区域、图片裁切和底部固定栏遮挡。
- 继续对 15 个页面做微信开发者工具视觉验收，并根据验收结果补齐页面级适配问题。
- 对照 `design/UI-ACCEPTANCE-CHECKLIST.md` 继续修正安全区、底部固定栏和视觉细节；TabBar 主入口与固定操作栏叠加问题已在源码层修正，仍需开发者工具实机确认。
- 登录页验证码倒计时、手机号/验证码校验和提交禁用已在源码层补齐，仍需在开发者工具中确认键盘弹起后的输入框与主按钮可达性。
- 服务评价页的评分、标签、内容校验、防重复提交、图片上传中拦截、图片选择、预览、删除、失败重试和上传 URL 回填已接入后端 Mock；提交成功后返回订单 TabBar 的跳转方式已修正，真实文件传输、CDN 存储和上传失败错误码仍需后续接入。
- 我的页最近预约和退出登录二次确认已在源码层补齐，仍需开发者工具确认卡片触控区域、底部 TabBar 留白和 Modal 展示。
- 门店列表页搜索、排序、营业中筛选、定位异常状态、系统定位和地图选点入口已补齐；`wx.getLocation` / `wx.chooseLocation`、权限拒绝、取消和重算距离展示仍需微信开发者工具/真机确认。
- 服务列表页门店切换、分类、搜索、排序和空态恢复已补齐；仍需开发者工具确认横向分类滚动、长服务名换行和 TabBar 底部留白。
- 服务详情页收藏、原生分享、门店摘要、注意事项、推荐技师和当前服务评价分页列表已补齐；评价提交后可在内存 Mock 列表回流展示，分享能力仍需开发者工具/真机确认，生产评价持久化和审核状态仍需后续接口接入。
- 门店详情页 Tabs、展开区块、swiper 图片轮播、门店动作反馈和评价分页列表已补齐；评价提交后可在内存 Mock 列表回流展示，地图导航、电话呼出和轮播手势仍需开发者工具/真机确认；真实门店图片资源、生产评价持久化和审核状态仍需后续接口接入。
- 选择技师页可约/不可约、自动分配互斥、草稿恢复和底部摘要已补齐；源码层已增加双固定区域底部留白，仍需开发者工具确认不同机型安全区间距。
- 选择时间页日期/时段/号源联动、草稿时段恢复、已选摘要和无可约时间操作入口已补齐；源码层已增加双固定区域底部留白，仍需开发者工具确认不同机型安全区间距，以及后续真实日期查询接口接入。
- 订单列表页完整 Tabs、关键动作入口、动作重复点击锁和取消状态同步已补齐；取消预约、支付参数准备、Mock 支付确认和再次预约草稿已接入后端接口，后续仍需接入真实微信支付商户参数与回调。
- 预约详情页状态 Steps、预约码说明、支付信息、动作入口、动作重复点击锁和底部签到按钮动作锁已补齐；取消预约、支付参数准备、Mock 支付确认、改期草稿、改期提交、预约码刷新和联系门店已接入接口/小程序能力，真实微信支付商户参数与回调仍需后续接入。
- 到店签到页验证码刷新接口、刷新码并发锁、重复签到提示、底部动作反馈和二维码图片 URL 回填已补齐；导航到店和电话呼出已接入微信能力，真实动态二维码图片生成与小程序渲染仍需后续真机确认。
- 预约成功页预约码说明、导航/联系和结果动作已补齐；导航到店和电话呼出已接入微信能力，支付结果通知仍需后续小程序能力/接口接入。
- 首页快捷入口、附近门店排序和会员权益点击反馈已补齐；客服入口已接入小程序原生 `contact` 能力，仍需微信开发者工具/真机确认客服会话配置和失败兜底表现。
- 确认预约页门店、项目、技师、时间修改入口已补齐；门店和项目修改已在源码层联动预约草稿并重置旧技师/时间，后续仍需在开发者工具中确认从各修改页返回后的草稿展示一致性。

### 管理后台核心流程

- 预约管理继续增强：改期、更换技师、分配房间时需要做技师和房间冲突校验。
- 到店核销和服务订单继续增强：需要补齐失败态、重复点击禁用、批量操作和操作审计展示。
- 本轮推进：后台 Mock API 增加预约操作审计记录；服务订单页增加操作记录 Drawer，到店核销页增加可选客户批量签到入口。
- 本轮推进：后台 Mock 登录会话增加角色、门店范围和过期时间；布局按角色过滤菜单，过期会话自动回登录页，退出时清理结构化会话。
- 本轮推进：后台新增 `mock/dev/prod` 服务出口并将总部运营看板接入 `/api/v1/admin/dashboard` 的 Remote 映射；其他后台页面继续使用 Mock，按页面逐步替换。
- 本轮推进：统一后台远程看板的门店排名字段语义，将后端返回的比较值明确为到店率百分比，并补充 MockMvc 契约测试。
- 本轮推进：总部看板技师利用率改为 Mock/Remote 契约字段，页面不再直接维护技师利用率数据；后端看板接口和契约测试已同步补齐。
- 本轮推进：排班与资源页接入后台 `mock/dev/prod` 服务出口，可读取 `/api/v1/admin/schedule-resources` 并映射技师周排班、房间状态和后端状态码。
- 新增页面目前为 Mock 表格和详情 Drawer，后续需要补齐创建/编辑表单校验、空态、错误态和批量操作。

### Mock 契约统一

- 客户端、后台、后端仍各有 Mock 数据文件，需要抽出统一业务命名和状态映射表。
- 继续统一预约状态展示：`待支付 / 已预约 / 已签到 / 待服务 / 服务中 / 待结算 / 已完成 / 已取消`；门店营业状态和选择时间段已完成状态码/展示标签分离。
- Mock 数据补齐各状态样例，并覆盖空态、错误、禁用、约满和冲突场景。
- 前端页面不应直接维护最终字典；Mock 阶段字典已继续向 service/mock 层集中，后端已提供 `/api/v1/catalog/client` 承接主要客户端字典、运营文案、通用动作反馈、页面状态文案、门店/服务列表排序项、公共卡片 meta 文案、门店/服务详情页区块说明、预约链路动态单位文案、技师卡 meta 文案、订单列表结构化 Tabs、订单详情支付字段、预约码说明、评价页展示/校验文案、登录页文案、技师状态标签和成功页动作文案。页面层 WXML/TS 已完成展示文案与 UI 占位字集中收敛，剩余中文主要在 Mock 展示数据、后端 Mock 字典和真实业务数据字段中。

## 3. P1 剩余工作

- 后端接口契约继续补齐：登录 Mock 和用户信息已覆盖，会员/优惠券/套餐仍为预留；开始服务、完成服务和结算状态接口已完成，改期后续需要增强持久化资源占用释放/重占和并发锁。
- 后端 domain 增强：显式状态流转、房间占用、技师排班、服务准备时间和清洁时间冲突校验已在 Mock 层覆盖；正式数据库接入仍需将应用服务临界区替换为事务、行锁/唯一约束，并补齐真实房间容量和排班规则。
- 数据库 DDL 需要在具备 MySQL 客户端或测试环境后真实执行验证。
- 管理后台登录态守卫、Mock 过期时间和角色菜单边界已补齐；后续仍需接入真实认证、服务端会话校验和细粒度接口权限。
- 客户端后续接入真实 CDN 或本地图片资源，并在微信开发者工具中检查图片比例、裁切和加载失败表现。
- 客户端远程服务已有本地 HTTP smoke 脚本验证主要接口，并覆盖支付准备、预约码、改期、取消、评价图片和评价回流；客户端已支持通过 `qiyu-api-mode` 切换 dev/prod，后续仍需在微信开发者工具中执行真实网络联调，并结合真实微信支付结果、服务端回调和通知规则深化成功页业务。

## 4. P2 后续增强

- 支付、退款、结算和会员权益完整规则。
- 经营报表深度分析和导出能力。
- 测试环境部署、接口联调、CI 和验收报告。

## 5. 下一步建议

优先继续推进客户端 UI 主线。下一批建议聚焦微信开发者工具中的 15 页视觉验收，以及根据验收结果修正安全区、滚动遮挡、图片比例和底部固定栏；后端资源冲突和真实接口联调保持为并行后续工作。
