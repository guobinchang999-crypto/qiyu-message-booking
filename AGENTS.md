# AGENTS.md

## 文档入口

在对本项目进行重要修改前，优先阅读以下文档：

1. `README.md`：了解产品范围、技术栈、模块结构和开发入口。
2. `DEVELOPMENT-PLAN.md`：了解开发顺序、Mock 策略、阶段目标和验收标准。
3. `design/UI-DESIGN-SYSTEM.md`：了解客户端 UI 设计规范、令牌、组件和页面映射。
4. `design/CODEX-UI-DEVELOPMENT-PROMPT.md`：了解微信小程序客户端的实现约束和交付要求。
5. `design/UI-ACCEPTANCE-CHECKLIST.md`：了解 UI 验收标准、适配要求和问题记录格式。
6. `skills/cola-architecture/SKILL.md`：开发或审查 `qiyu-server` 后端架构前必须阅读。

如果后续新增后端架构、数据库设计、后台 UI 或部署相关技能文档，执行相关任务前也应先阅读对应文档。

使用仓库技能时：

- `skills/cola-architecture/SKILL.md`：用于 `qiyu-server` 后端分层、包结构、领域建模和架构审查。

## Agent Focus

- 保持栖愈作为统一品牌按摩预约系统，不扩展无关业务模型。
- 项目代码规划为三个模块：`qiyu-server`、`qiyu-admin`、`qiyu-client`。
- 优先完成可运行、可演示、可验收的预约闭环，再扩展复杂营销、财务和报表能力。
- 测试环境未搭建完成前，所有接口交互默认使用 Mock 数据。
- 兼顾开发体验和最终用户体验，避免只做静态页面或只做后端接口而忽略完整流程。
- 任何 UI 开发都应先对齐 `design/` 目录中的设计规范和效果图。

## 产品边界

- 所有门店属于统一品牌体系。
- 前端页面不展示门店经营类型。
- 不出现多经营主体、门店经营类型或门店独立会员体系等字段或文案。
- 会员余额、优惠券和套餐权益统一表达为“全门店通用”。
- 首页推荐顺序固定为：常去门店 → 附近门店。
- 客户端品牌名称保持为“栖愈｜推拿·SPA”。

## 语言规则

- 项目文档以中文为主。
- 代码标识符使用清晰英文命名。
- 代码注释使用英文，重点解释意图、业务规则或非显而易见的实现。
- 面向用户的前端文案使用中文，并集中管理，避免散落硬编码。
- 不使用含混命名，例如 `data1`、`item2`、`container3`、`tempList`。

## 模块职责

### `qiyu-server`

- 提供统一后端 API。
- 负责登录认证、角色权限、门店、服务项目、技师、房间和排班。
- 负责预约创建、改期、取消、订单状态流转和资源冲突校验。
- 负责到店签到、核销、开始服务、完成服务和结算状态。
- 负责会员权益、优惠券、套餐卡、支付和退款的接口预留。
- 负责管理后台统计、运营看板和经营报表接口。

### `qiyu-admin`

- 使用 React、TypeScript、Ant Design Pro 和 Ant Design 5。
- 提供总部运营看板、门店管理、预约管理、到店核销、服务订单、排班管理、技师管理、服务项目、房间管理、客户管理、会员管理、优惠券和经营报表。
- 保持企业级后台布局，优先使用表格、筛选、抽屉、弹窗、统计卡片、状态标签和图表。
- 不使用偏营销落地页的视觉结构，不做过度装饰。

### `qiyu-client`

- 使用微信小程序原生框架、TypeScript 和 TDesign MiniProgram。
- 覆盖 15 个客户端页面：登录、首页、门店列表、门店详情、服务列表、服务详情、选择技师、选择时间、确认预约、预约成功、订单列表、预约详情、到店签到、服务评价、我的。
- 必须用真实 WXML/WXSS/TDesign 组件实现页面。
- 不允许把整张 UI 效果图当作页面底图。
- 图片缺失时使用比例一致的占位资源，不从截图中粗暴裁切文字或控件。

## 后端规则

- API 直接使用根路径下的业务资源地址，不增加 `/api/v1` 等统一版本前缀。
- Controller 保持轻薄，只负责参数接收、权限入口和响应转换。
- 业务编排放在 application service，核心规则放在 domain。
- 数据库实体不直接返回给前端。
- 外部输入必须校验。
- 预约必须校验技师、房间、服务时长、准备时间、清洁时间和营业时间。
- 状态流转必须显式，不允许任意字符串状态在各层流动。
- 软删除、审计字段、创建时间和更新时间应统一处理。

## 前端规则

- 管理后台优先使用 Ant Design Pro / Ant Design 组件，不重复造基础控件。
- 微信小程序优先使用 TDesign MiniProgram 组件和主题令牌。
- 客户端视觉以 `design/UI-DESIGN-SYSTEM.md` 为最高优先级，其次参考对应效果图，再参考 TDesign 默认行为。
- 页面之间的门店、服务项目、技师、预约时间、订单状态和金额必须保持一致。
- 底部导航、固定操作区、弹窗和 Toast 必须处理安全区。
- 触控区域不小于 44px。
- 长列表需要考虑分页、加载中、空状态、错误状态和弱网反馈。
- 表单必须处理必填校验、错误提示、重复提交禁用和提交中状态。

## 业务状态规则

预约和订单状态统一为：

```text
待支付
已预约
已签到
待服务
服务中
待结算
已完成
已取消
```

新增状态前必须同步更新：

- 后端枚举或常量
- API 文档
- 管理后台状态标签
- 小程序状态标签
- Mock 数据
- UI 验收清单

## 设计与实现边界

- 设计图是视觉基准，不是可直接复用的页面实现。
- 业务流程尚未确定的部分，只保留 Mock 接口和事件入口。
- 当前测试环境未就绪时，不因真实接口缺失阻塞客户端和后台开发。
- 不擅自补造真实支付、退款、会员结算或复杂财务规则。
- 不接入真实第三方服务，除非任务明确要求。
- 不引入与既定技术栈冲突的大型框架。

## 代码质量规则

- 保持简单、清晰、可测试。
- 避免过深嵌套和巨大通用基类。
- 避免控制器、页面组件或状态文件膨胀成 god object。
- 复用组件应基于真实重复场景，不为抽象而抽象。
- 命名应体现业务含义，例如 `BookingStatusTag`、`TherapistCard`、`TimeSlotSelector`。
- 重要流程应有最小可用测试或可演示 Mock 数据。

## Guardrails

- 不要在 Controller 中堆业务 if/else。
- 不要让前端直接依赖数据库表结构。
- 不要跳过预约资源冲突校验。
- 不要跳过后台权限边界。
- 不要硬编码大量 UI 颜色、间距和状态色。
- 不要把客户端效果图作为整页背景实现。
- 不要在客户端出现门店类型、多经营主体或独立会员体系文案。
- 不要让 UI 图层、组件、文件或变量出现 `容器1`、`组件2`、`页面3` 这类不可维护命名。

## 工作流

开发新功能时：

1. 先确认功能属于 `qiyu-server`、`qiyu-admin` 还是 `qiyu-client`。
2. 阅读相关设计文档和现有代码。
3. 明确输入、输出、状态和异常场景。
4. 优先实现可演示的最小闭环。
5. 补充必要 Mock 数据、类型定义和状态处理。
6. 运行对应模块的构建、类型检查或最小验收。
7. 更新 README、接口说明或设计文档中受影响的内容。

## Definition of Done

- 相关模块可以正常构建或运行。
- 核心流程可以通过 Mock 或真实接口演示。
- UI 与设计规范保持一致。
- 状态、错误、空态、加载和禁用表现完整。
- 关键业务规则被执行，特别是预约状态和资源冲突。
- 文档已随行为变化同步更新。
