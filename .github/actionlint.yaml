# Keep custom self-hosted runner labels explicit so workflow linting can verify
# that qiyu-test is intentional rather than a misspelled hosted runner name.
self-hosted-runner:
  labels:
    - qiyu-test
